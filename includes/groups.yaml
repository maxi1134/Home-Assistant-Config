# ─── Groups Configuration ───
# Custom groups for organizing entities. Used by notification and presence logic.
all_users:
  name: All users
  entities:
    - person.maximiliano
    - person.myriam