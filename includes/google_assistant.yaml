  project_id: !secret google_project_id
  service_account: !include google.json
  secure_devices_pin: !secret google_secure_devices_pin
  report_state: true
  exposed_domains:
    - switch
    - lock
    - climate
    - vacuum
    - fan
    - light
    - cover
    - camera
    - sensor
    - alarm_control_panel
    - input_boolean
  # entity_config:
    # media_player.hotbox_one:
      # name: HotBox One
      # expose: true
      # aliases:
        # - Xbox_One