# Node-RED

This repo no longer tracks the full Node-RED flow exports that older documentation linked to.

## Archived Flows

The documentation still references the old flow categories for context, but the JSON exports themselves are not committed here anymore.

Use the screenshots in [example_flow.png](../assets/node-red/example_flow.png), [example_flow_2.png](../assets/node-red/example_flow_2.png), and [example_flow_microwave.png](../assets/node-red/example_flow_microwave.png) as the remaining published references.
