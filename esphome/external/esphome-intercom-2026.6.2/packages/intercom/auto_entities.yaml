# Auto-create the standard intercom_api entities for minimal native-audio
# profiles. The component only emits entities that match the configured audio
# direction: mic_gain for mic paths, master_volume for speaker paths.
intercom_api:
  auto_entities: true
