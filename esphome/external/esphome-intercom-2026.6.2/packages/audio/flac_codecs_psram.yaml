# =============================================================================
# ESPHome audio decoder memory policy for full-featured PSRAM profiles.
#
# Keep FLAC decoder buffers out of internal RAM. Do not include this package in
# light AEC profiles where flash/heap budget is intentionally tighter.
# =============================================================================

audio:
  codecs:
    flac:
      buffer_memory: psram
