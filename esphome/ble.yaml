esphome:
  name: ble
  friendly_name: BLE

esp32:
  board: esp32dev
  framework:
    type: arduino

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: !secret api_key_bl

ota:
  password: "36a71c8b04216d1435924e28e0f30960"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Ble Fallback Hotspot"
    password: "5AvmbGBk0kvm"

captive_portal:
    

bluetooth_proxy:
  active: true