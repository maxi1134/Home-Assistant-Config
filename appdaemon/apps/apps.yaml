---
secrets: secrets.yaml
hello_world:
  module: hello
  class: HelloWorld
# TODO: This preserves existing PTZ token behavior, but it exposes the camera token
# via Home Assistant state. Replace with a private in-memory flow before removing it.
get_token_app:
  module: get_token
  class: GetToken
  user: homeassistant
  password: !secret get_token_app
  url: http://192.168.0.210/cgi-bin/api.cgi?cmd=Login&token=null
